pipeline {
    agent any
    
    stages {
        stage('Build by Manan') {
            steps {
                // Run build commands
                sh 'echo "Building project..."'
            }
        }
        
        stage('Test by Manan') {
            steps {
                // Run tests
                sh 'echo "Running tests..."'
            }
        }
        
        stage('Deploy by Manan') {
            steps {
                // Deploy step (dummy for now)
                sh 'echo "Deploying application..."'
            }
        }

        stage('Ending Stage by Manan') {
            steps {
                // Deploy step (dummy for now)
                sh 'echo "End of Pipeline by Manan"'
            }
        }
    }
}
